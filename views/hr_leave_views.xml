<odoo>
    <record id="view_hr_leave_form" model="ir.ui.view">
        <field name="name">hr.leave.form</field>
        <field name="model">hr.leave</field>
        <field name="arch" type="xml">
            <form string="Leave Request">
                <header>
                    <button string="Submit for Review" type="object" name="action_in_review" states="draft"/>
                    <button string="Confirm" type="object" name="action_confirm" states="draft"/>
                    <button string="Approve" type="object" name="action_approve" states="in_review" groups="dh_hr_leave.group_hr_leave_manager"/>
                    <button string="Refuse" type="object" name="action_refuse" states="in_review" groups="dh_hr_leave.group_hr_leave_manager"/>
                    <button string="Cancel" type="object" name="action_cancel" states="draft,confirm,in_review,approved"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,confirm,in_review,approved,refused,cancel" readonly="1"/>
                </header>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="employee_id"/>
                        <field name="start_date"/>
                        <field name="end_date"/>
                        <field name="description"/>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="activity_ids" widget="mail_activity"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>

    <record id="view_hr_leave_tree" model="ir.ui.view">
        <field name="name">hr.leave.tree</field>
        <field name="model">hr.leave</field>
        <field name="arch" type="xml">
            <tree string="Leave Requests">
                <field name="name"/>
                <field name="employee_id"/>
                <field name="start_date"/>
                <field name="end_date"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="action_hr_leave" model="ir.actions.act_window">
        <field name="name">Leave Requests</field>
        <field name="res_model">hr.leave</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
